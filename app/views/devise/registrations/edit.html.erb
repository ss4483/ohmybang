<div class="box" style="height:100%;">
  <div class="container h-100" style="position: relative; padding-top: 146px;">

    <div class="row d-flex justify-content-center mt-5 mx-0"style="width:100%; min-height:600px; padding-bottom:120px;">
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="col-12 col-sm-6 col-lg-5 col-xl-8" style="margin-bottom:40px;" >
        <p class="text-center" style="border-bottom: 1px solid rgba(0, 0, 0, 0.1); padding-bottom: 12px; font-size: 40px; font-weight: 600;">회원 정보 수정</p>
      </div>
      <div class="w-100" style="height:0px;"></div>
      <div class="col-12 col-sm-6 col-lg-5 col-xl-4" >

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
          <%#= render "devise/shared/error_messages", resource: resource %>

          <div class="field">
            <%#= f.email_field :email, autofocus: true, autocomplete: "email" %>
            <input class="form-control mb-3" type="email" value="<%=current_user.email%>" style="max-width:350px; margin-left: auto; margin-right: auto;" disabled >
          </div>

          <div class="field">
            <%= f.label :password, "비밀번호 변경" %> <%#<i>(leave blank if you don't want to change it)</i><br />%>
            <%= f.password_field :password, autocomplete: "new-password", class: "form-control mb-3", style: 'max-width:350px; margin: auto; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"; ', placeholder: "비밀번호 (최소 #{@minimum_password_length} 자리 이상 설정해 주세요)" %>
          </div>

          <div class="field">
            <%#= f.label :password_confirmation %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control my-3", style: 'max-width:350px; margin: auto; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";', placeholder: "비밀번호 확인" %>
          </div>

          <div class="field">
            <%= f.label :current_password, "현재 비밀번호" %> <%#<i>(we need your current password to confirm your changes)</i><br />%>
            <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control mb-3", style: 'margin: auto; max-width:350px; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";', required: true %>
          </div>

          <div class="field">
            <p style="margin-bottom: 0.5rem;">이벤트 등 프로모션 알림 메일 수신 동의</p>
            <div class="text-right">
              <div class="form-check form-check-inline">
                <input class="form-check-input" type="radio" name="user[is_promotion]" id="is_promotion_y" value="t" <%= "checked" if current_user.is_promotion == "t" %>>
                <label class="form-check-label" for="is_promotion_y">예</label>
              </div>
              <div class="form-check form-check-inline">
                <input class="form-check-input" type="radio" name="user[is_promotion]" id="is_promotion_n" value="f" <%= "checked" if current_user.is_promotion == "f" %>>
                <label class="form-check-label" for="is_promotion_n">아니오</label>
              </div>
            </div>
          </div>

          <div class="actions">
            <%= f.submit "수정하기", class: "btn-block btn btn-green mt-3", style: "padding: 10px;" %>
          </div>
        <% end %>
        <div class="text-right mt-3">
          <%= link_to '회원 탈퇴', "/users", class: "btn btn-outline-danger btn-sm", data: {title: "정말로 탈퇴 하시겠습니까?",confirm: '확인 시 즉시 탈퇴가 됩니다.<br>탈퇴 후 포인트 및 뷰어는 즉시 제거되므로 신중히 생각해주세요.<br>', verify: current_user.email, verify_text: "아래에 본인 이메일(#{current_user.email})을 적어주세요", commit: "탈퇴", cancel: "취소", method: "delete"} %>
        </div>
      </div>


    </div>
  </div>
</div>