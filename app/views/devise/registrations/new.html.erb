<div class="box" style="height:100%; ">
  <div class="container h-100" style="position: relative; padding-top: 146px;">

    <div class="row d-flex justify-content-center mt-5 mx-0" style="width:100%; padding-bottom: 120px;">
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="col-12 col-sm-6 col-lg-5 col-xl-8" style="margin-bottom: 40px;" >
        <p class="text-center" style="border-bottom: 1px solid rgba(0, 0, 0, 0.1); padding-bottom: 12px; font-size: 40px; font-weight: 600;">회원가입</p>
        <img class="d-block" src="<%= image_url("sign_up_2_step.png")%>" style="width:100px; margin: 15px auto;"/>
        <p class="text-center">이메일과 비밀번호를 작성해주세요.</p>
      </div>
      <div class="w-100"></div>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: "col-12 col-sm-6 col-lg-5 col-xl-4" }) do |f| %>
        <%#= render "devise/shared/error_messages", resource: resource %>
        <input type="hidden" name="user[is_promotion]" value="<%= (params[:p] == "t")? "t" : "f" %>">
        <div class="field" style="margin: auto; max-width:350px;">
          <%#= f.label :email %>
          <%= f.email_field :email, class: "form-control", autofocus: true, autocomplete: "email", placeholder: "이메일을 입력해주세요.", style: '', required: true %>
          <p style="font-size:12px; color: #bbbbbb">(실제 사용하는 이메일 주소를 입력해 주세요.)</p>
        </div>

        <div class="field">
          <%#= f.label :password %>
          <%# if @minimum_password_length %>
          <!--<em>(<%#= @minimum_password_length %> characters minimum)</em>-->
          <%# end %>
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control my-3", placeholder: "비밀번호를 입력해주세요." , style: 'margin: auto; max-width:350px; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";', required: true %>
        </div>

        <div class="field mb-3" style="margin: auto; max-width:350px; ">
          <%#= f.label :password_confirmation %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control mt-3", placeholder: "비밀번호를 확인해주세요." , style: 'font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";', required: true %>
          <p style="font-size:12px; color: #bbbbbb">영문, 대·소문자, 숫자, 특수문자(8~16 자)</p>
          <%# <p style="font-size:12px; color: #bbbbbb">대소문자는 구분되며 특수문자는 !@#$%^&*()+=~_-만 사용 가능합니다.</p> %>
        </div>
        

        <div class="actions">
          <%= f.submit "회원가입", class: "btn-block btn btn-green btn-lg mt-3 p-2" %>
        </div>
      <% end %>

    </div>
  </div>          
</div>

